import React from 'react';
import { loader } from '../assets';

const Loader = ({}) => {
    return (
        <div className="fixed inset-0">

        </div>
    );
};

export default Loader;
// by Rokas with ❤️
